<div class="booyah-box-b col-10 offset-1">
  <h1><%= "Destination Page" %></h1>

<!DOCTYPE html>
<html>
  <head>
    <title>Simple Map</title>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
  <body>
    <div id="map"></div>
    <script>
      var map;
      function initMap() {
        map = new google.maps.Map(document.getElementById('map'), {
          center: {lat: <%= @destination.latitude %>, lng: <%= @destination.longitude %>},
          zoom: 8
        });
      }
    </script>

    <script async defer
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCNEGq63BFjbRviLiWGPXBEI9Sp0iuAve8&callback=initMap">
    </script>
  </body>
  </html>

    <h3><%= link_to @destination.name, traveler_trip_destination_path, class: 'row text-center' %></h3>

    <h6 class="text-center"><'insert pic thumbnails here'></h6>
    <h6 class="text-center"><'inser journal entries with dates'></h6>


    <br />
</div>
